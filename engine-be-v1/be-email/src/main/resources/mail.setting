# 邮件服务器的 SMTP端口，可选，默认25
port = 465
# 发件人（必须正确，否则发送失败）
from = from
# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码）
pass = pass
# 使用 STARTTLS安全连接，STARTTLS是对纯文本通信协议的扩展。
starttlsEnable = true
# 使用 SSL安全连接
sslEnable = true
